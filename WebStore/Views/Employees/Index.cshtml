@model IEnumerable<EmployeeViewModel>

@{
    //ViewData["Title"] = "Сотрудники";
    ViewData["title"] = "Сотрудники";
}

    <div class="container">
        <h3>@ViewData["title"]</h3>
        <a asp-action="Edit" class="btn btn-success">Создать через Edit</a>
        <a asp-action="Create" class="btn btn-success">Создать через Create</a>
        <hr />

        <table class="table table-bordered" id="employees-table">
            <thead>
                <tr>
                    <th>id</th>
                    <th>Фамилия</th>
                    <th>Имя</th>
                    <th>Отчество</th>
                    <th>Возраст</th>
                    <th>Карточка</th>
                    <th>Редактировать</th>
                    <th>Удалить</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var employee in Model)
                {
                <tr>
                    <td>@employee.Id</td>
                    <td>@employee.SecondName</td>
                    <td>@employee.Name</td>
                    <td>@employee.Patronymic</td>
                    <td>@employee.Age</td>
                    <td>
                        <a asp-controller="Employees" asp-action="Details" asp-route-Id="@employee.Id">Карточка</a>
                    </td>
                    <!--<td>
        <a asp-controller="Employees" asp-action="Edit1" asp-route-Id="@employee.Id">Изменить</a>
    </td>-->
                    <td>
                        <a asp-controller="Employees" asp-action="Edit" asp-route-Id="@employee.Id">Edit</a>
                    </td>
                    <td>
                        <a asp-controller="Employees" asp-action="Delete" asp-route-Id="@employee.Id" class="text-danger">Delete</a>
                    </td>
                </tr>
                }
            </tbody>
        </table>
    </div>

@section Styles{
    <style>
        #employees-table {
            background: red
        }
    </style>
}

@section Scripts
{
    <!-- <script>QWE</script> -->
}
